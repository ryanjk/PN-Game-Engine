#version 450 core

in vec2 tc;

layout (binding = 0) uniform sampler2D splashscreen;

uniform float alpha;

out vec4 color;

void main(void) {
	color = texture(splashscreen, tc, 0) * vec4(1.0, 1.0, 1.0, alpha);
//	color = vec4(1.0,1.0,1.0,1.0);
}